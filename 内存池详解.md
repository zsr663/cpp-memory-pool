# 内存池（Memory Pool）代码详解

> 本文档配套讲解同目录下的 `memoryMgr.h` 和 `memoryMgr.cpp`。
> 这是一个**固定大小内存池（Fixed-size Memory Pool）** 的完整实现，采用经典的 **空闲链表（Free List）** 结构。

---

## 目录

1. [为什么需要内存池](#1-为什么需要内存池)
2. [整体架构](#2-整体架构)
3. [核心数据结构：MemoryBlock](#3-核心数据结构memoryblock)
4. [单个内存池：MemoryAlloc](#4-单个内存池memoryalloc)
5. [全局内存管理器：MemoryMgr](#5-全局内存管理器memorymgr)
6. [完整分配与释放流程](#6-完整分配与释放流程)
7. [内存布局示意图](#7-内存布局示意图)
8. [设计要点与优点](#8-设计要点与优点)
9. [潜在问题与注意事项](#9-潜在问题与注意事项)
10. [学习要点总结](#10-学习要点总结)

---

## 1. 为什么需要内存池

`new` / `malloc` 每次调用都要向操作系统申请内存，存在以下开销：

- **系统调用开销**：频繁的 `malloc` / `free` 需要进出内核态，成本较高。
- **内存碎片**：频繁分配不同大小的内存，会导致内存被切得七零八落（外部碎片）。
- **分配效率不稳定**：`malloc` 内部要在空闲链表里查找合适的块，耗时不确定。

内存池的解决思路是：**一次性向系统申请一大块内存，之后自己在这块内存上做快速分配/回收**。

- 分配 = 从空闲链表取一个节点（O(1)）
- 释放 = 把节点插回空闲链表头部（O(1)）

本例用 **多个固定规格的池**（64 / 128 / 256 / 512 / 1024 字节），每次分配时向上取到"刚好装得下"的规格，属于简化版的 **slab 分配器** 思想。

---

## 2. 整体架构

代码分三层：

```
┌─────────────────────────────────────────────┐
│  MemoryMgr  （全局内存管理器，单例）          │
│  负责：根据请求大小选择合适规格的池            │
│      持有 5 个不同规格的 MemoryAlloc          │
└───────────────┬─────────────────────────────┘
                │  map<unsigned, MemoryAlloc*>
                │  6→64  7→128  8→256  9→512  10→1024
┌───────────────▼─────────────────────────────┐
│  MemoryAlloc （单个固定大小内存池）           │
│  负责：管理一块连续大内存，串成空闲链表        │
└───────────────┬─────────────────────────────┘
                │  每个内存单元头部带一个 MemoryBlock
┌───────────────▼─────────────────────────────┐
│  MemoryBlock （内存块头部）                   │
│  负责：记录空闲链表指针 + 所属内存池          │
└─────────────────────────────────────────────┘
```

- `MemoryBlock`：每个内存单元前面的**头部（Header）**，占 16 字节（64 位下两个指针）。
- `MemoryAlloc`：一个固定 `unitSize` 的内存池，内部是一块大内存 + 一条空闲链表。
- `MemoryMgr`：单例，管理 5 个池，对外提供统一的 `allocMem` / `freeMem` 接口。

---

## 3. 核心数据结构：MemoryBlock

`memoryMgr.h` 第 8~13 行：

```cpp
class MemoryAlloc;
class MemoryBlock
{
public:
    MemoryBlock* pNext = nullptr;   // 空闲链表的下一个节点
    MemoryAlloc* pAlloc = nullptr;  // 指向"所属的内存池"
};
```

`MemoryBlock` 是每个内存单元前面都带的一个**头部结构体**。它有两个作用：

| 字段 | 作用 |
|------|------|
| `pNext` | 把空闲块串成链表。分配时弹出头部，释放时插回头部。 |
| `pAlloc` | 记录这块内存属于哪个 `MemoryAlloc`。释放时据此找到对应的池；如果是 `malloc` 直接分配的（不属于任何池），则为 `nullptr`，释放时直接 `free`。 |

**关键点**：`pNext` 和 `pAlloc` 复用了"空闲块本身的内存空间"来存储链表信息——因为空闲块此时还没被用户使用，头部这 16 字节随便用。这就是 free-list 的核心技巧：**空闲时不额外申请管理结构，直接借用块内存**。

> 在 64 位系统上，两个指针各 8 字节，`sizeof(MemoryBlock) == 16`。

---

## 4. 单个内存池：MemoryAlloc

### 4.1 成员变量（`memoryMgr.h` 第 26~29 行）

```cpp
char* pMemoryAlloc;         // 内存池整块内存起始地址
MemoryBlock* pBlockHeader;  // 空闲链表头指针
unsigned unitCount;         // 内存池总块数量
unsigned unitSize;          // 每一块"用户可用"内存大小
```

### 4.2 构造函数（`memoryMgr.cpp` 第 7~12 行）

```cpp
MemoryAlloc::MemoryAlloc(unsigned unitCount_, unsigned unitSize_)
    : unitCount(unitCount_), unitSize(unitSize_),
    pMemoryAlloc(nullptr), pBlockHeader(nullptr)
{}
```

只记录参数，把指针置空。真正的内存申请放在 `initMemoryAlloc()` 里。

### 4.3 析构函数（第 15~19 行）

```cpp
MemoryAlloc::~MemoryAlloc()
{
    if (pMemoryAlloc)
        free(pMemoryAlloc);
}
```

只释放那一整块大内存。因为整块内存是用一次 `malloc` 申请的，所以用一次 `free` 就能全部归还。

### 4.4 初始化：initMemoryAlloc（第 22~47 行）

```cpp
void MemoryAlloc::initMemoryAlloc()
{
    assert(pMemoryAlloc == nullptr);           // 只能初始化一次
    pMemoryAlloc = (char*)malloc(unitCount * (unitSize + sizeof(MemoryBlock)));
    if (!pMemoryAlloc) {
        std::cout << "MemoryAlloc::initMemoryAlloc malloc fail" << std::endl;
        return;
    }
    pBlockHeader = (MemoryBlock*)pMemoryAlloc;
    for (unsigned i = 0; i < unitCount; ++i)
    {
        MemoryBlock* pMemoryBlock = (MemoryBlock*)(pMemoryAlloc +
            i * (unitSize + sizeof(MemoryBlock)));
        pMemoryBlock->pAlloc = this;           // 记录所属池
        if (i != unitCount - 1)
            pMemoryBlock->pNext = (MemoryBlock*)(pMemoryAlloc +
                (i + 1) * (unitSize + sizeof(MemoryBlock)));  // 指向下一块
        else
            pMemoryBlock->pNext = nullptr;      // 最后一块，指向空
    }
}
```

**做了什么：**

1. 一次性 `malloc` 一整块连续内存，大小为 `unitCount × (unitSize + 头部大小)`。
   - 注意：每个单元除了给用户用的 `unitSize` 字节，还要额外留 `sizeof(MemoryBlock)` 字节放头部。
2. 从起始地址开始，把这一整块内存**切分成 `unitCount` 个小单元**，用 `pNext` 把它们串成一条空闲链表。
3. 每个单元的头部 `pAlloc` 都指向 `this`（所属池），供将来释放时定位。

> `assert(pMemoryAlloc == nullptr)` 保证这个方法只会被调用一次，防止重复初始化导致内存泄漏。

### 4.5 分配：allocMem（第 50~68 行）

```cpp
void* MemoryAlloc::allocMem(size_t nSize)
{
    if (!pBlockHeader)
    {
        // 池内空闲块耗尽 → 直接 malloc 一块（不属于池，pAlloc 置空）
        MemoryBlock* pMemoryBlock = (MemoryBlock*)((char*)malloc(nSize
            + sizeof(MemoryBlock)));
        pMemoryBlock->pAlloc = nullptr;
        pMemoryBlock->pNext = nullptr;
        return (char*)pMemoryBlock + sizeof(MemoryBlock);
    }
    else
    {
        // 取空闲链表头部一块返回，头指针后移
        char* pReturn = ((char*)pBlockHeader + sizeof(MemoryBlock));
        pBlockHeader = pBlockHeader->pNext;
        return pReturn;
    }
}
```

**两种情况：**

- **空闲链表非空**（正常情况）：把链表头节点取出，`pBlockHeader` 后移，返回"头部之后"的用户可用地址。
  - 返回地址 = 块起始地址 + `sizeof(MemoryBlock)`，即跳过头部。
- **空闲链表为空**（池用完了）：退化为普通 `malloc`，同样多申请 `sizeof(MemoryBlock)` 放头部，但把 `pAlloc` 置 `nullptr`，表示"这块不属于任何池"。这样释放时 `MemoryMgr` 能识别并直接 `free`。

### 4.6 释放：freeMem（第 71~76 行）

```cpp
void MemoryAlloc::freeMem(void* block)
{
    MemoryBlock* pMemoryBlock = (MemoryBlock*)((char*)block - sizeof(MemoryBlock));
    pMemoryBlock->pNext = pBlockHeader;   // 插到链表头部
    pBlockHeader = pMemoryBlock;
}
```

- 用户传进来的是"用户数据地址"，往前退 `sizeof(MemoryBlock)` 字节就是头部。
- 把该块**插回空闲链表头部**（头插法，O(1)）。
- 不做任何清零、合并（固定大小池不需要合并）。

---

## 5. 全局内存管理器：MemoryMgr

### 5.1 单例模式（`memoryMgr.h` 第 36~43 行 + `memoryMgr.cpp` 第 79~83 行）

```cpp
static MemoryMgr& getInstance();                       // 获取唯一实例
MemoryMgr(const MemoryMgr& memoryMgr) = delete;        // 禁止拷贝
MemoryMgr& operator= (const MemoryMgr& memoryMgr) = delete; // 禁止赋值
private:
    MemoryMgr();                                       // 构造私有化
```

```cpp
MemoryMgr& MemoryMgr::getInstance()
{
    static MemoryMgr instance;   // 局部静态变量，首次调用时构造
    return instance;
}
```

这是 **Meyers 单例**（局部静态变量实现）：C++11 起，局部 `static` 变量的初始化是线程安全的，且只在首次调用 `getInstance()` 时构造一次。同时 `delete` 掉拷贝构造和赋值，防止出现多个实例。

### 5.2 成员变量（`memoryMgr.h` 第 44~49 行）

```cpp
std::map<unsigned, MemoryAlloc*> memoryAllocMap;
MemoryAlloc memoryAlloc64;     // 64 字节 × 10 块
MemoryAlloc memoryAlloc128;    // 128 字节 × 10 块
MemoryAlloc memoryAlloc256;    // 256 字节 × 10 块
MemoryAlloc memoryAlloc512;    // 512 字节 × 10 块
MemoryAlloc memoryAlloc1024;   // 1024 字节 × 10 块
```

5 个不同规格的池，每个池 10 块。`memoryAllocMap` 用 `log2(unitSize)` 作为 key 建立索引，方便根据大小快速找到池。

### 5.3 构造函数（第 86~104 行）

```cpp
MemoryMgr::MemoryMgr()
    :memoryAlloc64(10, 64),
    memoryAlloc128(10, 128),
    memoryAlloc256(10, 256),
    memoryAlloc512(10, 512),
    memoryAlloc1024(10, 1024)
{
    memoryAlloc64.initMemoryAlloc();
    // ... 其余 4 个同理
    memoryAllocMap.insert(std::pair<unsigned, MemoryAlloc*>(6, &memoryAlloc64));
    memoryAllocMap.insert(std::pair<unsigned, MemoryAlloc*>(7, &memoryAlloc128));
    memoryAllocMap.insert(std::pair<unsigned, MemoryAlloc*>(8, &memoryAlloc256));
    memoryAllocMap.insert(std::pair<unsigned, MemoryAlloc*>(9, &memoryAlloc512));
    memoryAllocMap.insert(std::pair<unsigned, MemoryAlloc*>(10, &memoryAlloc1024));
}
```

构造时先初始化 5 个池（各自 malloc 一块大内存并串好空闲链表），再把它们按 key 装入 map。

> key 含义：`64 = 2^6`，所以 64 字节池的 key 是 6；`128 = 2^7` → 7；…… `1024 = 2^10` → 10。

### 5.4 分配：allocMem（第 107~131 行）

```cpp
void* MemoryMgr::allocMem(size_t nSize)
{
    if (nSize <= MAX_UNIT_SIZE)   // MAX_UNIT_SIZE = 1024
    {
        unsigned num = 32;
        unsigned i = 6;
        while ((num = num * 2) < nSize)
            ++i;
        return memoryAllocMap[i]->allocMem(nSize);
    }
    else
    {
        // 大于 1024 → 直接 malloc，不走内存池
        MemoryBlock* pMemoryBlock = (MemoryBlock*)((char*)malloc(nSize + sizeof(MemoryBlock)));
        if (!pMemoryBlock) return nullptr;
        pMemoryBlock->pAlloc = nullptr;
        pMemoryBlock->pNext = nullptr;
        return (char*)pMemoryBlock + sizeof(MemoryBlock);
    }
}
```

**核心逻辑：向上取到"刚好装得下"的规格。**

`while ((num = num * 2) < nSize)` 这段是在找满足 `2^i >= nSize` 的最小的 `i`（最小为 6）：

| 请求大小 `nSize` | 循环过程 | 最终 key | 实际分配池 |
|-----------------|---------|---------|-----------|
| 1 ~ 64          | num=64，64<64 不成立 | 6 | 64 字节 |
| 65 ~ 128        | num=64<65 成立→i=7，num=128 不成立 | 7 | 128 字节 |
| 129 ~ 256       | … | 8 | 256 字节 |
| 257 ~ 512       | … | 9 | 512 字节 |
| 513 ~ 1024      | … | 10 | 1024 字节 |
| > 1024          | 不进 if 分支 | — | 直接 malloc |

**两种结果：**

- `nSize <= 1024`：算出 key，交给对应池的 `allocMem`。
- `nSize > 1024`：直接 `malloc`（带头部，`pAlloc = nullptr`）。

### 5.5 释放：freeMem（第 134~147 行）

```cpp
void MemoryMgr::freeMem(void* block)
{
    MemoryBlock* pMemoryBlock = (MemoryBlock*)((char*)block - sizeof(MemoryBlock));
    if (!pMemoryBlock->pAlloc)
        free(pMemoryBlock);              // 不属于任何池 → 直接 free
    else
        pMemoryBlock->pAlloc->freeMem(block);  // 还给对应池
}
```

释放时通过头部里的 `pAlloc` 判断：

- `pAlloc == nullptr` → 这块是 `malloc` 直接分配的，直接 `free` 整块（含头部）。
- `pAlloc != nullptr` → 这块属于某个池，调用该池的 `freeMem` 把块插回空闲链表。

> 这个设计让"池内内存"和"池外直接 malloc 的内存"统一用同一套 `allocMem` / `freeMem` 接口，调用方无需区分。

---

## 6. 完整分配与释放流程

**分配 `malloc(100)` 字节：**

```
MemoryMgr::allocMem(100)
  → 100 <= 1024，进入池分支
  → num=32, i=6
     循环：num=64<100 成立 → i=7；num=128<100 不成立 → 结束，i=7
  → memoryAllocMap[7] = &memoryAlloc128
  → memoryAlloc128.allocMem(100)
      → 空闲链表非空 → 弹出头节点，返回用户地址
```

**释放这个指针：**

```
MemoryMgr::freeMem(ptr)
  → header = ptr - 16
  → header->pAlloc == &memoryAlloc128（非空）
  → memoryAlloc128.freeMem(ptr)
      → 头插回空闲链表
```

**分配 `malloc(2048)` 字节（超规格）：**

```
MemoryMgr::allocMem(2048)
  → 2048 > 1024 → 直接 malloc(2048 + 16)
  → pAlloc = nullptr，返回用户地址

MemoryMgr::freeMem(ptr)
  → header->pAlloc == nullptr → free(header)
```

---

## 7. 内存布局示意图

以 `memoryAlloc64`（unitSize=64，unitCount=10）为例，64 位系统下 `sizeof(MemoryBlock)=16`。

### 7.1 整块大内存的切分

一次性 malloc 的大小 = `10 × (64 + 16) = 800` 字节，被切成 10 个等大的单元：

```
pMemoryAlloc（整块起始）
│
▼
┌──────────────────────────────────────────────────────────────┐
│ 块0 (80B) │ 块1 (80B) │ 块2 (80B) │ ... │ 块9 (80B)        │
└──────────────────────────────────────────────────────────────┘
   偏移 0      偏移 80      偏移 160         偏移 720
```

### 7.2 单个内存单元的布局

```
        ←──── sizeof(MemoryBlock) = 16 ────→  ←── unitSize = 64 ──→
        ┌──────────────────┬────────────────┐ ┌───────────────────┐
        │  pNext (8B)      │  pAlloc (8B)   │ │   用户可用数据     │
        └──────────────────┴────────────────┘ └───────────────────┘
        ^                  ^                 ^   ^
        │                  │                 │   └─ allocMem 返回的指针指向这里
        │                  │                 └───── 块起始 + 16
        │                  └─ 记录所属池
        └─ 空闲时指向下一块
```

### 7.3 空闲链表（初始化后）

```
pBlockHeader
│
▼
[块0]──> [块1]──> [块2]──> ... ──> [块9]──> nullptr
```

- 分配：取走 `块0`，`pBlockHeader` 指向 `块1`。
- 释放 `块0`：`块0` 头插，`pBlockHeader` 又指回 `块0`。

---

## 8. 设计要点与优点

1. **O(1) 分配与释放**：只操作链表头部，无查找、无遍历。
2. **一次申请、批量管理**：5 个池各只 malloc 一次，减少系统调用。
3. **无外部碎片**：固定大小切分，块之间不会产生无法利用的碎片。
4. **头部复用内存**：空闲块的头部直接借用块自身空间存链表信息，不额外申请管理内存。
5. **统一接口**：`MemoryMgr` 屏蔽了"池内/池外"差异，调用方只用一套 `allocMem` / `freeMem`。
6. **借力单例**：全局唯一，构造时集中初始化所有池。

---

## 9. 潜在问题与注意事项

1. **不负责对象构造/析构**：池只管理**裸内存**，不会调用构造函数和析构函数。若要存放 C++ 对象，需配合 **placement new** 和 **显式析构** 使用。
2. **分配大小超池会越界**：`MemoryAlloc::allocMem` 内部没有校验 `nSize <= unitSize`。它依赖 `MemoryMgr` 提前按 2 的幂取整保证安全；若绕过 `MemoryMgr` 直接调 `memoryAlloc64.allocMem(1000)`，会拿到 64 字节的块，写入 1000 字节就会**缓冲区溢出**。
3. **重复释放（double free）**：`freeMem` 不做校验，同一块 free 两次会破坏空闲链表，导致同一块被重复分配，造成严重 bug。
4. **线程不安全**：`allocMem` / `freeMem` 没有加锁，多线程并发分配/释放会出错。若需多线程，要加互斥锁或改造成无锁结构。
5. **没有内存合并/归还系统**：池申请后不会因为空闲而把大内存还给操作系统（除非析构）；池耗尽后 `allocMem` 退化的那块 `malloc` 内存在 `free` 后能正常归还，但池内空闲块不会归还系统。
6. **对齐问题**：本例各 `unitSize`（64/128/256/512/1024）恰好是 16 的倍数，加上 16 字节头部，返回地址仍能保持 16 字节对齐；但这是"碰巧"成立，代码本身并未显式保证对齐，若要存需要严格对齐的类型（如 `long double`、SIMD 类型），应谨慎。
7. **析构顺序**：`MemoryMgr` 是单例，其 5 个 `MemoryAlloc` 成员在程序结束时会自动析构，各自 `free` 自己的大内存——前提是所有分配出去的内存都已被释放，否则仍有泄漏（本例无引用计数/泄漏检测）。
8. **内存不重用（池外退化部分）**：当某个池 10 块用完后，`allocMem` 退化走 `malloc`，释放时直接 `free` 回系统，而不是补充进池——这会导致池子用完后该规格失去池化收益。

---

## 10. 学习要点总结

| 概念 | 在代码中的体现 |
|------|---------------|
| **free-list 空闲链表** | `MemoryAlloc::initMemoryAlloc` 把块串成链表 |
| **头部（Header）** | `MemoryBlock`，每块前面 16 字节 |
| **头插法** | `freeMem` 把块插回链表头 |
| **单例模式（Meyers）** | `MemoryMgr::getInstance` 的局部 static |
| **禁止拷贝** | `= delete` 拷贝构造与赋值 |
| **向上取整到 2 的幂** | `allocMem` 里 `while ((num = num*2) < nSize)` |
| **map 建立规格索引** | key = `log2(unitSize)` |
| **池外退化** | 池耗尽或超规格时直接 `malloc`，用 `pAlloc==nullptr` 标记 |
| **C 风格内存管理** | 全程 `malloc` / `free`，不 `new` / `delete` |

**一句话总结**：这个内存池用"整块大内存 + 空闲链表 + 分规格管理"的方式，把频繁的内存分配/释放降到 O(1)，并用头部的 `pAlloc` 指针把"池内内存"和"池外直接 malloc 的内存"统一到同一套接口里，是一个非常适合入门学习的经典内存池实现。
